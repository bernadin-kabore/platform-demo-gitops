# Discovers every apps/<name>/application.yaml in this repo and creates the
# matching ArgoCD Application for it. Add a new platform component by
# dropping a new directory under apps/ — nothing else needs to change here.
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: platform-apps
  namespace: argocd
spec:
  goTemplate: true
  generators:
    - git:
        repoURL: https://github.com/bernadin-kabore/platform-demo-gitops.git
        revision: main
        files:
          - path: "apps/**/config.json" # matches nested dirs too, e.g. apps/observability/kiali
  template:
    metadata:
      name: "{{.component}}"
    spec:
      project: platform
      source:
        repoURL: https://github.com/bernadin-kabore/platform-demo-gitops.git
        targetRevision: main
        # .path.path, not .path. A git *file* generator sets `path` to a
        # structured object — {path, basename, filename, segments} — which
        # silently overrides any "path" key inside config.json itself.
        # Referencing the bare object stringifies the whole map into
        # spec.source.path, and every Application reports "app path does not
        # exist" with a Go map literal in the message.
        path: "{{.path.path}}"
      destination:
        server: https://kubernetes.default.svc
        namespace: "{{.namespace}}"
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
          # Kyverno's clusterpolicies and policies CRDs are larger than the
          # 262144-byte ceiling on metadata.annotations, and a client-side
          # apply stores the whole manifest in
          # kubectl.kubernetes.io/last-applied-configuration. Both CRDs are
          # rejected, every ClusterPolicy that depends on them then fails with
          # "ensure CRDs are installed first", and the platform ends up
          # enforcing no admission policy at all while the Application merely
          # looks OutOfSync. Server-side apply does not write that annotation.
          - ServerSideApply=true
        retry:
          limit: 5
          backoff:
            duration: 30s
            factor: 2
            maxDuration: 5m
