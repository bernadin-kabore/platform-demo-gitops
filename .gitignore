# Terraform
**/.terraform/
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
crash.log

# Node
node_modules/
npm-debug.log*

# Backstage
platform-demo-backstage/dist-types/
platform-demo-backstage/packages/*/dist/

# Editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Charts pulled by `kustomize build --enable-helm` during local rendering.
# ArgoCD fetches its own copies server-side; these are a local cache only.
charts/
